pipeline {
    agent any
  tools {
      maven 'maven'
  }
    stages {
        stage('build'){
            steps{
                sh 'mvn clean'
                sh 'mvm install'
                sh 'mvn package'
                echo ' build'
                
            }
        }
    }
}
